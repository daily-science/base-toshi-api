[flake8]
max-line-length = 120
max-complexity = 18
ignore = E203, E266, W503, D
docstring-convention = google
per-file-ignores = base_toshi_api/**/__init__.py: F401
    base_toshi_api/tests/*.py: D100,D101,D102,D103,E501
    base_toshi_api/schema/*.py: D102
exclude = .git,
    __pycache__,
    setup.py,
    ANLY,
    bin,
    build,
    dist,
    docs,
    lib,
    releases,
    .venv,
    .tox,
    .dynamodb,
    .mypy_cache,
    .pytest_cache,
    .vscode,
    .github,
    .serverless,
    s3_extract,
    node_modules,
    # By default test codes will be linted.
    base_toshi_api/tests

[mypy]
ignore_missing_imports = True

[coverage:run]
# uncomment the following to omit files during running
omit = base_toshi_api/tests/*

[coverage:report]
exclude_lines =
    pragma: no cover
    def __repr__
    if self.debug:
    if settings.DEBUG
    raise AssertionError
    raise NotImplementedError
    if 0:
    if __name__ == .__main__.:
    def main

[tox:tox]
isolated_build = true
envlist = audit, py312, format, lint

[gh-actions]
python =
    3.12: py312, format, lint

[testenv]
allowlist_externals = pytest
extras =
    test
passenv = *
setenv =
    PYTHONPATH = {toxinidir}
    PYTHONWARNINGS = ignore
    TESTING = 1
    SLS_OFFLINE = 1
commands =
    # pytest base_toshi_api tests
    pytest --cov-config=setup.cfg --cov=base_toshi_api --cov-branch --cov-report=xml --cov-report=term-missing

[testenv:audit]
allowlist_externals =
	poetry
commands =
    poetry export --all-groups --output audit.txt
    poetry run pip-audit -r audit.txt --require-hashes
	poetry run safety scan


[testenv:format]
allowlist_externals =
    isort
    black
extras =
    test
commands =
    isort base_toshi_api
    black base_toshi_api
    flake8 base_toshi_api
    # mypy base_toshi_api